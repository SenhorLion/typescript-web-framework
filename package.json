{
  "name": "typescript-web-framework",
  "description": "An Example of building a web framework using Typescript.",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:db": "json-server --watch db.json",
    "start:dev": "parcel index.html",
    "start": "concurrently npm:start:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "concurrently": "^4.1.1"
  },
  "devDependencies": {
    "typescript": "^3.5.2"
  }
}